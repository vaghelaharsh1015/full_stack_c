#include<stdio.H>
using namespace std;
int  main()
{
    int arr[]={10,-10,10,10,10};
    int num=0;

    for (int i = 0; i < 6; i++)
    {
        if (arr[i]<0)
        {
            arr[i]==num;
        }        
    }
     cout<<"zero is = "<<arr[i];
     
    return 0;
}