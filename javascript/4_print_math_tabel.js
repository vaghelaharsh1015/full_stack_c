"use strict";

const ps = require("prompt-sync");
const prompt = ps();

let num,multiply;
 num = parseInt(prompt(`enter a value:`));

for (let i = 1; i <= 10; i++) {
    // multiply= num * i;
    console.log(num +"*"+ i+":"+mul);
}
