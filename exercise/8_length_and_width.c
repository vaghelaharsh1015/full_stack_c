#include<stdio.h>
int main()
{
    int length,width,rectangle;
    printf("enter a length :");
    scanf("%d", & length);

    printf("enter a width :");
    scanf("%d",& width);
 rectangle = length*width;

printf (" arer is %d ", rectangle);
    
    return 0;
}